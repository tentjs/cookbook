name: Build on push

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run: npm run build
